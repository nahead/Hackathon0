# AI Employee System - Cloud Deployment (Minimal Dependencies)
# Optimized for Python 3.13 compatibility

# Core System Dependencies (Cloud Deployment)
python-dotenv==1.0.0
requests==2.31.0
flask==3.1.3
anthropic==0.18.1

# Note: railway_all_in_one.py uses only standard library modules
# All other dependencies are for local development and MCP servers